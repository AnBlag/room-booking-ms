spring:
  datasource:
    password: 123
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/room
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
server:
  port: 8084
  error:
    include-message: always

sql:
  query:
    batch-update: UPDATE department SET name_department=?, position=? WHERE id=?

